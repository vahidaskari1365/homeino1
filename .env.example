# ============================================================
# Homeino AI — provider configuration (all optional)
# Nothing configured → heuristic LLM + mock image provider,
# and every result is honestly labeled "پیش‌نمایش".
# ============================================================

# --- LLM Service: any OpenAI-compatible /chat/completions endpoint ---
# LLM_API_BASE_URL=https://api.freellmapi.com/v1
# LLM_API_KEY=
# LLM_MODEL=glm-4.7-flash

# --- Orali: real image-edit + overlay-region engine ---
# ORALI_API_BASE_URL=https://api.orali.com
# ORALI_API_KEY=
# ORALI_MODEL=orali-room-v1

# --- Base image provider (pre-existing slots) ---
# GEMINI_API_KEY=
# GEMINI_TEXT_MODEL=gemini-2.5-flash
# GEMINI_IMAGE_MODEL=gemini-2.5-flash-image
# FREELLMAPI_API_KEY=
# FREELLMAPI_BASE_URL=
