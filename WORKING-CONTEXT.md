# WORKING-CONTEXT — حافظه ماندگار پروژه هومینو

> الگو برگرفته از ECC. هر ایجنت پیش از شروع کار این فایل را می‌خواند و پس از اتمام، بخش «وضعیت» را به‌روز می‌کند.
> آخرین به‌روزرسانی: 2026-09-09 (فیکس AI عکس روی Vercel)

## وضعیت فعلی (چه چیزی کار می‌کند)

- **پروداکشن روی Vercel زنده است**: `homeino.vercel.app` (دامنه `homeino.ir` هنوز DNS ندارد — کاربر باید در Vercel دامنه را وصل کند)
- **فیکس AI عکس (کامیت db73735)**: ریشه باگ «قسمت AI عکس درست نمیده» — با صفر کلید، provider انتخابی mock بود و mock هرگز خطا نمی‌داد، پس زنجیره fallback هرگز pollinations را صدا نمی‌زد → تولید عکس = عکس استوک pexels با برچسب preview. فیکس: `imageDispatchPlan` (پروایدر.ts) + dispatch با plan در pipeline و /api/ai — حالا بدون هیچ کلیدی، تولید عکس واقعاً AI است (pollinations) و ویرایش صادقانه preview. ۶ تست طلایی `imageChain.test.ts` این باگ را قفل می‌کند (۱۷۳ تست سبز)
- **پس از فیکس، بدون کلید**: تولید عکس = AI واقعی رایگان (pollinations/sana) · ویرایش/استیجینگ = پیش‌نمایش صادقانه (بدون موتور ویرایش، هیچ سرویس رایگانی ندارد)
- **ایجنت‌های محتوای روزانه**: مجله (۳ نوبت/روز) و الهام (۳ نوبت/روز) — سبز و در حال انتشار (استخر ۷۳۱ + چرخش هوشمند، f7e8a0a)
- **نگهبان (watchdog-daily)**: دوبار در روز + اسکن امنیتی هفتگی agentshield + پنل اتمیشن ادمین

## گلوگاه‌ها و کارهای باز (به ترتیب اثر)

1. **envهای Vercel ناقص است** (تشخیص زنده: `/api/health` و `/api/ai/status`):
   - `DATABASE_URL` ← ست نیست؛ health قرمز (ok:false, db:not-configured) — لاگین/سفارش/اعتبار mock fallback می‌شوند
   - `GEMINI_API_KEY` (رایگان از Google AI Studio) ← برای ویرایش/استیجینگ واقعی لازم است — بدون آن تولید خوب است اما ویرایش mock می‌ماند
   - `NEXT_PUBLIC_SITE_URL=https://homeino.ir` + envهای Supabase (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`)
2. **دامنه homeino.ir** به Vercel وصل شود (DNS)
3. **تصمیم کلید xpl_** (Experiential Labs — کوتا خالی تا 2026-10-01): شارژ یا fallback آخر زنجیره (`scripts/lib/llm-chain.mjs`)
4. درگاه پرداخت ایرانی (زرین‌پال/آیدی‌پی) به‌عنوان provider جدید در `src/services/payments.ts`
5. فهرست ۱۰۰ تولیدکننده موج بنیان‌گذار + ایجنت جذب غرفه (الگوی `docs/agent-playbooks/new-daily-agent.md` آماده استفاده است)

## تصمیم‌های ثبت‌شده (دیگر بحث نشود)

- **زنجیره تصویر (db73735)**: generate = موتور واقعی → pollinations (کلید-کمتر) → mock صادقانه؛ edit = موتور واقعی → mock (pollinations ویرایش ندارد). mock هیچ‌وقت قبل از موتور واقعی/رایگان نمی‌آید — با تست قفل شده
- **استفاده از ECC به‌صورت الگو در ریپو** (نه لینک در سایت): AGENTS.md، WORKING-CONTEXT، agentshield، docs/agent-skills، کارخانه ایجنت روزانه — تایید کاربر 2026-09-09
- کارت قیمت لانچ: بنیان‌گذار ۱۰۰ غرفه اول ۱٫۵م/سال (۳٪) · رایگان (۵٪ تیکتی/۸٪ کالا) · حرفه‌ای ۳م/سال (۳٪) · ویژه ۵م/سال (۲٪) + سه ضمانت
- معماری پرداخت provider-based (Stripe پیاده، webhook، تسویه، گارد Dev)
- توکن گیت‌هاب تا **2026-12-08** اعتبار دارد؛ متغیر مخزن `PAT_EXPIRES_AT` + هشدار watchdog. ایجنت‌ها فقط به GITHUB_TOKEN داخلی وابسته‌اند

## قرارداد به‌روزرسانی این فایل

پس از هر کار بزرگ: وضعیت را تغییر بده، گلوگاه حل‌شده را حذف/علامت بزن، تصمیم جدید را در بخش تصمیم‌ها بنویس، تاریخ بالا را عوض کن. سابقه مفصل در `worklog.md` سندباکس می‌ماند — اینجا فقط وضعیت زنده.
